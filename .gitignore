# ── Binários compilados ──────────────────────────────────────────────────────
out/
src/sistema
src/sistema_dbg

# ── Resultados de simulação (arquivos binários grandes) ──────────────────────
simulations/results/

# ── Arquivos gerados automaticamente pelo opp_msgc ───────────────────────────
src/messages/*_m.cc
src/messages/*_m.h

# ── Tabelas e figuras derivadas pela análise ──────────────────────────────────
analysis/figures/

# ── IDE Eclipse / OMNeT++ IDE ────────────────────────────────────────────────
.cproject
.project
.settings/
.oppbuildspec

# ── Documentação de terceiros (copyrighted, pesada) ──────────────────────────
UserGuide.txt

# ── Ambiente local (segredos e caminhos de máquina) ──────────────────────────
.env

# ── Estado local do Qtenv ─────────────────────────────────────────────────────
simulations/.qtenvrc

# ── Graphify ──────────────────────────────────────────────────────────────────────
# O grafo, relatório, HTML e manifesto são compartilháveis; estado de
# máquina, intermediários, cache e custos permanecem locais.
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/.graphify_detect.json
graphify-out/.graphify_ast.json
graphify-out/.graphify_semantic.json
graphify-out/.graphify_analysis.json
graphify-out/.graphify_labels.json
graphify-out/.graphify_labels.json.sig
graphify-out/cache/
graphify-out/cost.json
graphify-out/20??-??-??/

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/

# ── Intermediários da documentação LaTeX/Markdown ────────────────────────────
docs/_markdown_*/
docs/*.aux
docs/*.log
docs/*.luabridge.lua
docs/*.out

# ── Estado local de aprendizado do graphify ──────────────────────────────────
graphify-out/.graphify_learning.json
graphify-out/.vocab.txt
graphify-out/memory/
graphify-out/reflections/

# ── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
*.swp
